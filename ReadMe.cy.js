//Info: Cypress is built on top of Mocha and the framework used is Mocha js

/**
 * Info: What kind of tests cypress runs
 * . End-to-end tests
 * . Component tests
 * . Integration tests
 * . Unit tests
*/

//Info: Install Node version Manager {NVM} and Install Node version v14.18.1 using NVM

/**
 * # git clone https://github.com/ {to install the git project}{https://github.com/Postavshik/ngx-cypress-test}
 * # npm install {To install all the dependencies in the package.json file]
 * # npm start {To run the Web Application on the local Host "localhost:4200"}
 * # npm install cypress
 * # npx cypress open {To run the Cypress Application}
 * ? Go to the cypress.config.ts file and configure cypress settings
 * ? Go to the dom.cy.js
 *    
 * 
*/

